spring.application.name=organization-query

spring.cloud.consul.discovery.tags=domain=organization,type=query

spring.cloud.config.uri=http://${CC_CONFIG_SERVER}
spring.cloud.config.name=${CC_CONFIG_NAME}
