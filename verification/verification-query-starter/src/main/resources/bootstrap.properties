spring.application.name=verification-query

spring.cloud.consul.discovery.tags=domain=verification,type=query

spring.cloud.config.uri=http://${CC_CONFIG_SERVER}
spring.cloud.config.name=${CC_CONFIG_NAME}
