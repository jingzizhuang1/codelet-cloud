spring.application.name=notification-command

spring.cloud.consul.discovery.tags=domain=notification,type=command

spring.cloud.config.uri=http://${CC_CONFIG_SERVER}
spring.cloud.config.name=${CC_CONFIG_NAME}
