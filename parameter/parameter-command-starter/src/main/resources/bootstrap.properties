spring.application.name=parameter-command

spring.cloud.consul.discovery.tags=domain=parameter,type=command

spring.cloud.config.uri=http://${CC_CONFIG_SERVER}
spring.cloud.config.name=${CC_CONFIG_NAME}
